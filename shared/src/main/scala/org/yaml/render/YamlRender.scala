package org.yaml.render

import org.yaml.lexer.YamlToken.Indent
import org.yaml.lexer.YeastToken
import org.yaml.model.{YNode, YPart, YTokens}

/**
  * Yaml Render
  */
object YamlRender {
//    /** Render a YamlPart as an String */
//    def render(part: YPart): String = {
//        val builder = new StringBuilder()
//        render(part, builder)
//        builder.toString
//    }
//
//    private def render(t: YeastToken, builder: StringBuilder): Unit = {
//        t.tokenType match {
//            case Indent => builder.append(" " * (t.end-t.start))
//            case _ =>
//        }
//    }
//
//    private def render(part: YPart, builder: StringBuilder):Unit = {
//        part match {
//            case n: YNode => render
//            case ts: YTokens => ts.tokens.foreach(render(_, builder))
//            case  =>
//        }
//
//    }

}

