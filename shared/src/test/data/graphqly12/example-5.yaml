%YAML 1.2
---
!!seq [
  !!map {
    ? !!str "operation"
    : !!str "query",
    ? !!str "fields"
    : !!seq [
      !!map {
        ? !!str "alias"
        : !!str "empireHero",
        ? !!str "name"
        : !!str "hero",
        ? !!str "arguments"
        : !!map {
          ? !!str "episode"
          : !!str "EMPIRE"
        },
        ? !!str "fields"
        : !!seq [
          !!map {
            ? !!str "name"
            : !!str "name"
          }
        ]
      },
      !!map {
        ? !!str "alias"
        : !!str "jediHero",
        ? !!str "name"
        : !!str "hero",
        ? !!str "arguments"
        : !!map {
          ? !!str "episode"
          : !!str "JEDI"
        },
        ? !!str "fields"
        : !!seq [
          !!map {
            ? !!str "name"
            : !!str "name"
          }
        ]
      }
    ]
  }
]
