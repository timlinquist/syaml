%YAML 1.2
---
!!seq [
  !!map {
    ? !!str "operation"
    : !!str "query",
    ? !!str "name"
    : !!str "HeroNameAndFriends",
    ? !!str "fields"
    : !!seq [
      !!map {
        ? !!str "name"
        : !!str "hero",
        ? !!str "fields"
        : !!seq [
          !!map {
            ? !!str "name"
            : !!str "name"
          },
          !!map {
            ? !!str "name"
            : !!str "friends",
            ? !!str "fields"
            : !!seq [
              !!map {
                ? !!str "name"
                : !!str "name"
              }
            ]
          }
        ]
      }
    ]
  }
]
