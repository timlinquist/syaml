%YAML 1.2
---
!!map {
  ? !!str "First occurrence"
  : !!str &anchor "Foo",
  ? !!str "Second occurrence"
  : *anchor,
  ? !!str "Override anchor"
  : !!str &anchor "Bar",
  ? !!str "Reuse anchor"
  : *anchor
}
